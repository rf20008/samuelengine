#include "Move.hpp"
#include "Errors.hpp"

bool Move::operator<(const Move& other) {
    // to be done by Joshua. Please create a total ordering of Move, and return whether, in that ordering, this is considered less than other

    throw NotImplementedError("Move::operator<(const Move& other) is not implemented yet");
}